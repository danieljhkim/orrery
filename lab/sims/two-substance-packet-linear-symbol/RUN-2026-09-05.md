# Validated run — 2026-09-05

## Outcome

The deterministic fixture passed all predeclared source, operator,
evolution, control, refinement, and stability gates. Its result-neutral verdict
is **refuted** for the scoped apparatus hypothesis: equal-speed tuning of the
existing two-density/gradient-flux Hamiltonian does not produce a Maxwell-like
propagating transverse sector.

This is evidence about the frozen apparatus, not a nature claim and not a
no-go theorem for a different emergent-gauge object. Principia should reconcile
its verification-only model-property rows against this report and
`assets/results.json`; this run does not edit that repository.

## Source and method

- Orrery source before this fixture: `35a3d47ebc650212e8e7eb59b13752db093f1c28`.
- Frozen packet apparatus SHA-256: `bd3d02d5182446b94159e5eaf5d777da575f476d2c1d3e88a552517a5b881fe4`.
- Merged Principia ORB-11219 specification: `38cd4a77d8f230f1fbe8a036c180f24919cacc0d`.
- Independent lane 1: a six-field species-basis Fourier matrix using the exact
  centered-difference symbol.
- Independent lane 2: isolated perturbations evolved with the actual nonlinear
  chemical potentials and kick-drift-kick discretization.
- No RNG was used. The seeds are exact named cosine modes.

## Results for reconciliation

- Six fields in two dimensions yield four propagating roots, grouped into two
  positive-frequency acoustic branches, and two zero-frequency modes. There is
  no algebraic or Gauss constraint.
- Every propagating eigenvector and evolved sound seed is longitudinal relative
  to the wavevector. The minimum measured longitudinal projector was
  `0.9999999999999997`.
- Equal `c_plus = c_minus = 1` makes the two vacuum sound eigenvalues
  degenerate but leaves their current parallel to the wavevector. It is scalar
  branch degeneracy, not Maxwell polarization evidence.
- All T0 arms remained `transverse-frozen`: normalized frequency bound `0.0`
  and relative discrete-curl drift `0.0` for each species.
- Vacuum `lambda=0.8` and `lambda=0` operator eigenvalues agreed exactly. The
  B0 mixed-background squared speeds agreed with time evolution to
  `0.003194733854391602` relative, within the declared `0.02` bound.
- Matrix/time frequency disagreement was at most
  `6.582604017773124e-05` in the primary evolution set. Three spatial and three
  temporal rungs show decreasing continuum and time-integration errors.
- The equal-magnitude `(5,0)` versus `(3,4)` anisotropy fell from
  `0.018187867168147243` at size 64 to `0.0046064205071892905` at size 128.
  This is lattice dispersion, not a new continuum mode.
- The explicit X0 indefinite-Hessian witness was classified
  `rejected-local-instability` and was not evolved. The admissible B0 runs only
  establish local stability; they do not cure the cubic Hamiltonian's global
  unboundedness.

## Exact rerun

```sh
PYTHONDONTWRITEBYTECODE=1 UV_CACHE_DIR=/tmp/orbit-uv-cache-11220 uv run lab/sims/two-substance-packet-linear-symbol/main.py --check-determinism
python3 lab/tools/build-gallery.py
git diff --check
```
