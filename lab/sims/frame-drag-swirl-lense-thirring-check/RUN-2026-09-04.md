# Run record — 2026-09-04

Task: ORB-11172

Status: complete. The tracked run reproduced the source-quoted toy law,
executed all three predeclared Lense–Thirring shape tests twice, and produced
byte-identical canonical JSON. The scientific verdict is mixed at the
component level and **KILL** for the full shape: radial falloff fails while
free-amplitude linearity and sign reversal pass.

Command:

```sh
PYTHONDONTWRITEBYTECODE=1 UV_CACHE_DIR=/tmp/orbit-uv-cache-11172 \
  uv run lab/sims/frame-drag-swirl-lense-thirring-check/main.py --check-determinism
```

## Recorded verdicts

- **KILL — radial falloff.** The default flat profile measured exponent
  `-1.0 ± 7.11e-15`, outside the predeclared `-3` target by
  approximately 2. The solid and whirlpool alternatives also failed, at
  approximately `0` and `-2`.
- **PASS — J-surrogate scaling.** The `u0` amplitude ladder measured exponent
  `1` within its ladder error. This tests a free toy amplitude, not a derived
  angular momentum.
- **PASS — sign reversal.** Signed `u0` samples were exactly odd at stored
  precision and vanished at zero.
- **NO SOURCE COUPLING — mass diagnostic.** Varying `GM` left `Omega`
  unchanged because the mass parameter is absent from the imposed flow law.

The canonical payloads at `results.json` and `runs/2026-09-04.json` were
confirmed byte-identical with `cmp`. No magnitude in physical units is
claimed.
