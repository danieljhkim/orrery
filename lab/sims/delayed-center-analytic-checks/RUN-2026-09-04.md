# Run record — 2026-09-04

Task: ORB-11169

Status: complete — all four predeclared checks passed, and an in-process rerun
produced byte-identical canonical JSON. The four order/resolution ladders,
tolerances, and pass/kill conditions were fixed in `main.py` and `README.md`
before the tracked artifact was generated.

Command:

```sh
PYTHONDONTWRITEBYTECODE=1 UV_CACHE_DIR=/tmp/orbit-uv-cache-11169 \
  uv run lab/sims/delayed-center-analytic-checks/main.py --check-determinism
```

## Recorded verdicts

- **PASS — Legendre-not-Laplace.** Fine order 128 reproduced coefficients
  `(-beta_g)^n`, `n=0..6`, to maximum absolute error `8.8813e-14`. The
  extracted normalized coefficients had zero radius spread at stored
  precision, and the inferred offset slope was `0.3000000000000042` for
  `beta_g=0.3`.
- **PASS — exterior Poisson dipole.** The five-rung Cartesian stencil reached
  profile relative L2 error `3.1951e-4`, dipole relative error `3.2085e-5`,
  and last-pair convergence order `2.00003`. The fine effective dipole-moment
  slope was `-0.1333291`, against exact `-0.1333333`.
- **PASS — retarded-direction curl.** The fine finite-difference curl converged
  to the nonzero analytic value. Fine circle work was at most `2.7756e-17`;
  generic loop work was nonzero on every beta rung, changed by at most
  `4.3697e-6` on the final quadrature refinement, and its two smallest-beta
  scaled values differed by `2.1261%`.
- **PASS — 2-beta-not-4-beta.** The finest contrast coefficient was
  `2.00031255`, at least 1.9 from 4, and its asymptotic error approached second
  order (`2.00068` on the last pair).

The canonical payloads at `assets/results.json` and `runs/2026-09-04.json`
were confirmed byte-identical with `cmp`.
