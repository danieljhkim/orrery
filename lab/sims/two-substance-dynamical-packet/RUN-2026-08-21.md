# Run record — 2026-08-21

Task: ORB-10940

Status: complete. The predeclared update rule and gates below were recorded
before the validated run; measured values were filled after the executable
passed its internal checks and byte-identical rerun comparison.

## Predeclaration

- Geometry: 2-D periodic `128 x 128` lattice, `0.75`-cell spacing over a
  `96 x 96` domain; 3-D is out of scope.
- Dynamics: Hamiltonian density and kick-drift-kick update exactly as stated in
  `README.md` and `main.py`; each substance changes only by a periodic discrete
  divergence.
- Model hypothesis: in the irrotational sector `j_s=grad(theta_s)`, so the
  relative flux is the gradient of `theta1-theta2`. Treating this relative
  phase/current channel and its stiffness as physical is inherited from
  ORB-10163. The symmetric `lambda n_plus n_minus^2/4` coupling is declared
  here and is not emergent.
- Initialization: direct localized relative packet (mode i), exact initial
  common-mode zero, amplitudes `0.02, 0.04, 0.06, 0.08`.
- Co-moving region: `|x-x_centroid| <= 3 sigma_x` and
  `|y| <= 3 sigma_y` at the final time.
- Coherence: propagate iff every ladder member retains more than 70% of its
  relative energy in that window and has translated-profile correlation above
  0.55. Otherwise the verdict is disperse and G2–G5 are not used as discovery
  claims.
- Scaling pass: smallest common-mode signal exceeds 100 instrument floors and
  the log-log exponent versus initial packet energy lies in `[0.8, 1.2]`.
- Convergence: finest grid and timestep speed changes each below 1%.
- Region relaxation: more than 80% of initial energy leaves the fixed central
  initialization region and both substance flux budgets close at their
  summation floor.
- Far monopole: the maximum global common-mode integral remains at or below the
  separately conserved summation floor.

## Measured result

- G1 conservation: maximum `n1` and `n2` total drift `1.8189894e-12` each;
  summation floor `6.5483619e-11`.
- G1 speed: grid sequence `(dx,speed) = (0.75,0.89311684),
  (0.50,0.90372356), (0.375,0.90746151)` at `dt=0.06`; finest relative
  change `0.00411912`. Timestep sequence `(dt,speed) = (0.09,0.89416591),
  (0.06,0.89311684), (0.03,0.89344767)` at `dx=0.75`; finest relative
  change `0.000370281`.
- G1 coherence verdict: **propagates with bounded spreading**. Across the four
  amplitudes, co-moving relative-energy retention is `0.957597–0.957629` and
  translated-profile correlation is `0.726699–0.727173`. Maximum fractional
  Hamiltonian drift is `1.31945e-6`.
- G2: maximum absolute global `n_plus` integral `4.2228721e-13`; maximum
  absolute global `n_minus` integral `7.8137496e-13`; both are below the
  `6.5483619e-11` instrument floor. Full sampled histories are in
  `assets/results.json`.
- G3 primary deliverable: ladder `(E, integral |n_plus|)` =
  `(0.0339291,0.0890481)`, `(0.135717,0.355923)`,
  `(0.305362,0.799831)`, `(0.542866,1.41945)`. The fitted law is
  `integral |n_plus| = 2.6143311 E^0.9987348`. The representative window has
  positive integral `+0.649461`, negative integral `-0.769994`, maximum
  `+0.00197789`, and minimum `-0.00489172`. **Pass reading:** a co-moving
  common-mode pocket plus compensating structure scales proportionally to
  energy, so this declared dynamics contains an `E/c^2`-type hook. The
  prefactor is only in lattice units and the nonlinear coupling remains an
  explicit hypothesis.
- G4 direct-initialization near field: `97.4647%` of initial energy departed
  the fixed region. Integrated `n1` inflow is `-8.31738` for an initial excess
  `+8.36531`; integrated `n2` inflow is `+8.21690` for an initial excess
  `-8.36531`. Closure residuals are `1.78e-15` and `-3.02e-14`. The finite-time
  residual common integral is `-0.100477`, a measured trailing relaxation
  structure rather than complete refill by the end time.
- G5: maximum far common monopole `4.2228721e-13`; verdict **null at instrument
  floor**, consistent with ORB-10164.

## Reproducibility

Two independent output directories were generated with the command below and
both `cmp` checks returned zero:

```sh
MPLCONFIGDIR=/tmp/orbit-10940-mpl XDG_CACHE_HOME=/tmp/orbit-10940-xdg-cache \
  uv --cache-dir /tmp/orbit-10940-uv-cache run \
  lab/sims/two-substance-dynamical-packet/main.py --output-dir <fresh-dir>
```

- `results.json` SHA-256 (both runs):
  `b820af87b995f1da44e50fdc56421307730636d77f5a9092172c2b0e9c65c4ef`
- `packet-budget.png` SHA-256 (both runs):
  `23dd122caf8bc30c72e79096cdf966615373327071da24def1233faf138c3c4e`

The tracked artifacts have those same hashes.
