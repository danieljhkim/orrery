# Run record — 2026-08-21

Task: ORB-10941

Status: complete. The controls and thresholds below were fixed before the
validated tracked run. Measured classifications are reported without changing
the gates.

## Predeclaration

- Coupling/amplitude matrix: `lambda = 0, 0.2, 0.5, 0.8` crossed with packet
  amplitudes `0.02, 0.04, 0.06, 0.08`; direct zero-common packet
  initialization is identical across arms.
- Time profiles: sample signed, absolute, positive, and negative y-integrated
  common/relative profiles; their centroids, widths, correlations, speeds, and
  common-minus-relative lag normalized by relative width.
- Attachment/wake gate: evaluate samples at `t >= 0.40 duration`. Attachment
  requires at least 80% resolved samples inside `0.75` widths with correlation
  at least `0.50` and fitted speeds within 10%. Wake requires at least 80%
  resolved late samples, median lag below `-0.75` widths, median correlation at
  least `0.50`, and common speed more than 10% below relative speed. Otherwise
  report mixed/detached; no outcome is forced.
- Primary convergence: rerun all four amplitudes at `lambda=0.8` on grids
  `128, 192, 256` and timesteps `0.09, 0.06, 0.03`. Converged means finest
  exponent shift `<=0.02`, prefactor change `<=5%`, normalized-lag shift
  `<=0.10`, and unchanged profile classification.
- Emission gate: run a defect-emission arm only if the declared fixture family
  contains an autonomous defect-to-relative-mode emission operator with a
  conservative defect-plus-field budget. Do not substitute direct packet
  initialization or forced target relaxation.
- Stability: audit the original Hamiltonian analytically on unrestricted mode
  fields and numerically over every sampled base-run state. Record density
  positivity, energy-density range, and finiteness. Add no stabilizer.
- Budgets: retain separate `n1`, `n2`, global `n_plus`, global `n_minus`, total
  energy, and fixed initialization-region records with numerical floors.

## Measured coupling controls

The zero-coupling maximum `integral |n_plus|` is `2.4221e-12`, below the
predicted/declared null floor `2.6193e-10`. The nonzero energy fits are:

| lambda | exponent | prefactor | max-amplitude common magnitude |
| ---: | ---: | ---: | ---: |
| 0.2 | 0.9999182 | 0.6145802 | 0.3336381 |
| 0.5 | 0.9994946 | 1.5343181 | 0.8329827 |
| 0.8 | 0.9987172 | 2.4486585 | 1.3295052 |

At fixed amplitude, the fitted response exponents versus nonzero lambda range
from `0.9974417` to `0.9998368`. The resolved common magnitude is therefore
linear in the inserted coupling and approximately linear in packet energy.

## Measured attachment/wake result

For the representative `lambda=0.8`, amplitude `0.08` run, all 39 late samples
resolve common content. The median lag is `-0.841291` packet widths and median
absolute-profile correlation is `0.701788`. Fitted common and relative profile
speeds are `0.672648` and `0.969589`. Verdict: **trailing wake**. The common
structure trails and propagates substantially more slowly than the relative
packet; it is not packet-attached under the declared gate.

## Measured convergence

| grid size | dx | exponent | prefactor | normalized lag | verdict |
| ---: | ---: | ---: | ---: | ---: | --- |
| 128 | 0.750 | 0.9987172 | 2.4486585 | -0.841291 | trailing wake |
| 192 | 0.500 | 0.9984560 | 2.4389633 | -0.813310 | trailing wake |
| 256 | 0.375 | 0.9983694 | 2.4606385 | -0.801455 | trailing wake |

The finest grid shifts are `8.6555e-5` in exponent, `0.0088088` relative in
prefactor, and `0.0118548` widths in lag: **converged**.

| dt | exponent | prefactor | normalized lag | verdict |
| ---: | ---: | ---: | ---: | --- |
| 0.09 | 0.9987153 | 2.4459387 | -0.848339 | trailing wake |
| 0.06 | 0.9987172 | 2.4486585 | -0.841291 | trailing wake |
| 0.03 | 0.9987177 | 2.4485986 | -0.844157 | trailing wake |

The finest timestep shifts are `4.3126e-7`, `2.4439e-5`, and `0.0028659`,
respectively: **converged**. Speed is reported on every rung but is not used as
a substitute for convergence of the primary observables.

## Emission and stability verdicts

Endogenous defect emission is **unsupported**. The missing operator is an
equation coupling an autonomous defect/core degree of freedom and its energy
to an outgoing relative current `J_minus` (or `n_minus` boundary flux), with a
closed defect-plus-field energy/refill budget. The existing defect fixture's
pinned analytic core and externally translated relaxation target do not supply
that operator. No source was added.

For nonzero lambda, minimizing the potential at fixed `n_minus=m` gives
`n_plus=-lambda m^2/(2 c_plus^2)` and
`V_min=c_minus^2 m^2/4-lambda^2 m^4/(16 c_plus^2)`, which is unbounded below.
The sampled base domain remained positive and finite: minimum `n1=0.975239`,
minimum `n2=0.960001`, and sampled Hamiltonian density
`1.0104e-19–0.00319987`. This does not stabilize the global Hamiltonian. No
bounded variant was tested or silently substituted.

## Budgets and reproducibility

Across the coupling/amplitude matrix, maximum `n1` and `n2` total drifts are
`1.8190e-12` against the `6.5484e-11` floor. The global common monopole is at
most `5.7854e-13`, the relative total at most `8.9528e-13`, and maximum
relative energy drift is `1.3194e-6`. Local absolute common content is not
interpreted as net gravitational mass.

Two independent output directories were generated with:

```sh
MPLCONFIGDIR=<fresh-dir> XDG_CACHE_HOME=<fresh-dir> UV_CACHE_DIR=<fresh-dir> \
  uv run lab/sims/two-substance-dynamical-packet/main.py --output-dir <fresh-dir>
```

Both `cmp` checks returned zero. Final artifact hashes:

- `results.json`: `2cb7616ad2abc6dc21f9f648c80a01c8684d0831f72c3805fbd9af703491a84b`
- `packet-budget.png`: `8c340e0e11dfae85027ef0400e4269e016aa38691f249639661896772dc5aed6`
